import { a as MiniKitContext, e as emptyContext } from "../../OnchainKitProvider-C29aWJbj.js";
import sdk from "@farcaster/frame-sdk";
import { useState, useContext } from "react";
const useMiniKit = () => {
  const [isFrameReady, setIsFrameReady] = useState(false);
  const context = useContext(MiniKitContext);
  if (context === emptyContext) {
    throw new Error("useMiniKit must be used within a MiniKitProvider");
  }
  const setFrameReady = async (readyOptions = {}) => {
    sdk.actions.ready(readyOptions);
    setIsFrameReady(true);
    return context;
  };
  return {
    setFrameReady,
    isFrameReady,
    context: context.context,
    updateClientContext: context.updateClientContext,
    notificationProxyUrl: context.notificationProxyUrl
  };
};
export {
  useMiniKit
};
//# sourceMappingURL=useMiniKit.js.map
