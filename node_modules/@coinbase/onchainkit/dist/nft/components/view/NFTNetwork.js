import { jsxs, jsx } from "react/jsx-runtime";
import { base } from "viem/chains";
import { useChainId } from "wagmi";
import { baseSvg } from "../../../internal/svg/baseSvg.js";
import { cn, color, text } from "../../../styles/theme.js";
const networkMap = {
  8453: {
    chain: base,
    icon: baseSvg
  }
};
function NFTNetwork({ className, label = "Network" }) {
  const chainId = useChainId();
  if (!chainId || !networkMap[chainId]) {
    return null;
  }
  const { chain, icon } = networkMap[chainId];
  return /* @__PURE__ */ jsxs(
    "div",
    {
      className: cn(
        text.label2,
        "flex items-center justify-between",
        className
      ),
      children: [
        /* @__PURE__ */ jsx("div", { className: cn(color.foregroundMuted), children: label }),
        /* @__PURE__ */ jsxs("div", { className: "flex items-center gap-1", children: [
          /* @__PURE__ */ jsx("div", { className: "h-4 w-4 object-cover", children: icon }),
          /* @__PURE__ */ jsx("div", { children: chain.name })
        ] })
      ]
    }
  );
}
export {
  NFTNetwork
};
//# sourceMappingURL=NFTNetwork.js.map
